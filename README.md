# netwerkavond

Studenten kunnen zich inschrijven voor het event.

## Status

Er staat op dit moment nog geen uitvoerbare app-code in deze repository (alleen `README.md` en `LICENSE`).
Daardoor kan er nog geen volledige functionele check (build, test, run) uitgevoerd worden.

## Klaar om te controleren zodra app-code is toegevoegd

Gebruik daarna minimaal deze checks:

1. Installeer dependencies.
2. Start de app lokaal.
3. Controleer de inschrijfflow end-to-end.
4. Draai tests en linting.
